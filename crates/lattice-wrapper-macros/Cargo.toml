[package]
name = "lattice-wrapper-macros"
version = "0.1.0"
edition = "2024"

[features]
wasm = ["dep:wasm-bindgen", "toolkit/wasm"]

[dependencies]
wasm-bindgen = { version = "0.2.100", optional = true }
toolkit = { path = "../toolkit", features = [] }
